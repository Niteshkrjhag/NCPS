# Antigravity Protocol
- Use `docs/context/` as the source of truth for the NCPS architecture.
- Always check `plan.md` to see what has happened previously.
- You are strictly forbidden from editing code until the 'Approval Status' in `plan.md` is updated to 'Approved' by the user.